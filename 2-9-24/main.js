// Create a function that can transform a number into a string.

//P: Insert any number as a parameter to be converted into a string.

//R: The number will be returned as a string spelled out with its corresponding letters.

//E: If the number 17 is inserted into the function, its string of 'seventeen' will be returned.

//P: A function is created with the purpose of taking in a number to be converted and returned as a string.

function numberToString(num){
    return num.toString();
}