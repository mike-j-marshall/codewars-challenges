// Complete the method that takes a boolean value and return a "Yes" string for true, or a "No" string for false.

//P: The function will take in boolean parameter.

//R: A string of either "Yes" or "No" will be returned depending on whether a boolean is taken in.

//E: If a boolean value is entered, it will be returned as either "Yes" for true, or "No" for false.

//P: A function is created to utilize the method that takes in a boolean value. "Yes" is returned for true and "No" is returned for false.

function boolToWord(bool){
    return bool ? 'Yes' : 'No';
}